# Notes App

Simple notes application with an Express backend and a vanilla HTML/CSS/JS frontend.

Quick start:

1. Install dependencies

```bash
npm install
```

2. Run in development (requires `nodemon`)

```bash
npm run dev
```

3. Or run normally

```bash
npm start
```

Open http://localhost:3000 in your browser.
